apiVersion: v1
kind: Service
metadata:
  name: ai-api-gateway-svc
spec:
  selector:
    app: ai-api-gateway
  ports:
    - name: api
      protocol: TCP
      port: 8100
      targetPort: 8100
    - name: ui
      protocol: TCP
      port: 7860
      targetPort: 7860
  type: LoadBalancer
